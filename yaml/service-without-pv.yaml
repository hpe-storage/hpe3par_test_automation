apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: hpe-storage
  labels:
    app: nginx-service
spec:
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
  selector:
    app: nginx-service